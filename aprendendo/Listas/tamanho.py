import random

lista = []
quantidade = int(input("quantas vezes vamos jogar?"))

for i in range(quantidade):
    lista.append(random.randint(1,6))

for i in range(1,6)
    print("a quantidade de %i obtidos foi de %i."%(i,lista.count(i)))
